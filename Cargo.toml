# SPDX-FileCopyrightText: 2024-2026 Temps Contributors
# SPDX-License-Identifier: MIT OR Apache-2.0

[workspace]
resolver = "2"
members = [
    # Application crates
    "crates/temps-cli",
    "crates/temps-vm-agent",
    # Library crates
    "crates/temps-core",
    "crates/temps-cloud-protocol",
    "crates/temps-cloud-client",
    "crates/temps-cloud",
    "crates/temps-ai",
    "crates/temps-ai-chat",
    "crates/temps-entities",
    "crates/temps-migrations",
    "crates/temps-database",
    "crates/temps-queue",
    "crates/temps-auth",
    "crates/temps-git",
    "crates/temps-presets",
    "crates/temps-logs",
    "crates/temps-deployer",
    "crates/temps-projects",
    "crates/temps-domains",
    "crates/temps-proxy",
    "crates/temps-providers",
    "crates/temps-query",
    "crates/temps-query-postgres",
    "crates/temps-query-s3",
    "crates/temps-query-redis",
    "crates/temps-query-mongodb",
    "crates/temps-deployments",
    "crates/temps-backup",
    "crates/temps-backup-core",
    "crates/temps-notifications",
    "crates/temps-monitoring",
    "crates/temps-credential-checks",
    "crates/temps-network",
    "crates/temps-analytics",
    "crates/temps-audit",
    "crates/temps-config",
    "crates/temps-geo",
    "crates/temps-analytics-backend",
    "crates/temps-analytics-events",
    "crates/temps-routes",
    "crates/temps-analytics-funnels",
    "crates/temps-analytics-session-replay",
    "crates/temps-infra",
    "crates/temps-error-tracking",
    "crates/temps-analytics-performance",
    "crates/temps-status-page",
    "crates/temps-static-files",
    "crates/temps-file-store",
    "crates/temps-import-types",
    "crates/temps-import",
    "crates/temps-import-docker",
    "crates/temps-import-kubernetes",
    "crates/temps-import-coolify",
    "crates/temps-import-dokploy",
    "crates/temps-import-caprover",
    "crates/temps-import-portainer",
    "crates/temps-import-kamal",
    "crates/temps-captcha-wasm",
    "crates/temps-teams",
    "crates/temps-webhooks",
    "crates/temps-email",
    "crates/temps-email-tracking",
    "crates/temps-dns",
    "crates/temps-dns-resolver",
    "crates/temps-vulnerability-scanner",
    "crates/temps-kv",
    "crates/temps-flags",
    "crates/temps-blob",
    "crates/temps-log-aggregator",
    "crates/temps-clickhouse",
    "crates/temps-otel",
    "crates/temps-telemetry",
    "crates/temps-environments",
    "crates/temps-screenshots",
    "crates/temps-embeddings",
    "crates/temps-plugin-sdk",
    "crates/temps-wireguard",
    "crates/temps-ai-agent-cli",
    "crates/temps-ai-gateway",
    "crates/temps-agents",
    "crates/temps-agents-mcp-proxy",
    "crates/temps-memory",
    "crates/temps-sandbox",
    "crates/temps-pty-agent",
    "crates/temps-git-credential",
    "crates/temps-agent",
    "crates/temps-edge",
    "crates/temps-preview-gateway",
    "crates/temps-external-plugins",
    "crates/temps-revenue",
    "crates/temps-observability",
    "crates/temps-metrics",
    "crates/temps-ai-api-tools",
    "crates/temps-mcp-server",
    "examples/example-plugin",
    "examples/lighthouse-plugin",
    "examples/indexnow-plugin",
    "examples/google-indexing-plugin",
]

[workspace.package]
# Linha de versao propria do fork nextlw. O sufixo nomeia a origem em vez de
# fingir compatibilidade com as pre-releases do upstream: a base e a mesma
# 0.1.0, mas a partir daqui as correcoes sao nossas e o release e independente.
version = "0.1.0-nextlw.1"
edition = "2021"
license = "MIT OR Apache-2.0"
authors = ["Temps Contributors"]
repository = "https://github.com/gotempsh/temps"
homepage = "https://github.com/gotempsh/temps"

[workspace.dependencies]
arc-swap = "1.7"
# ============================================================================
# Async Runtime & Concurrency
# ============================================================================
tokio = { version = "1.47.1", features = ["full"] }
tokio-util = { version = "0.7", features = ["codec"] }
futures = "0.3.31"
futures-util = "0.3.31"
async-trait = "0.1.68"
async-stream = "0.3"

# ============================================================================
# Serialization & Deserialization
# ============================================================================
serde = { version = "=1.0.228", features = ["derive", "rc"] }
serde_derive = "1.0"
serde_json = "1.0.140"
serde_yaml = "0.9"
serde_urlencoded = "0.7.1"
serde_bytes = "0.11.17"
serde_path_to_error = "0.1.20"
serde_test = "1.0.177"
serde-transcode = "1.1.1"
serde-vars = "0.2.0"
semver = "1.0.28"

# ============================================================================
# Error Handling & Logging
# ============================================================================
anyhow = "1.0.86"
thiserror = "2.0.17"
log = "0.4"
tracing = "0.1.40"
tracing-subscriber = { version = "0.3.18", features = ["env-filter", "std", "fmt", "registry"] }

# ============================================================================
# Date/Time & UUID
# ============================================================================
chrono = { version = "0.4.38", features = ["serde"] }
uuid = { version = "1.10.0", features = ["v4", "v5", "fast-rng", "macro-diagnostics"] }

# ============================================================================
# Database - ORM & Migrations
# ============================================================================
sea-orm = { version = "1.1.14", features = [
    "sqlx-sqlite",
    "sqlite-use-returning-for-3_35",
    "sqlx-postgres",
    "runtime-tokio-rustls",
    "macros",
    "with-chrono",
    "with-json",
    "with-uuid",
    "debug-print",
    "mock"
] }
sea-orm-migration = { version = "1.1", features = [
    "sqlx-sqlite",
    "sqlite-use-returning-for-3_35",
    "sqlx-postgres",
    "runtime-tokio-rustls"
] }
# Pinned to 0.8 to match sea-orm 1.1's sqlx: a direct 0.9 dep splits the graph
# into two sqlx versions and 0.9's SqlSafeStr guard breaks our dynamic queries.
sqlx = { version = "0.8", features = ["postgres", "mysql", "runtime-tokio"] }
rusqlite = { version = "0.32.0", features = ["bundled", "trace"] }

# ============================================================================
# HTTP Framework & Web Server
# ============================================================================
axum = { version = "0.8", features = ["multipart", "form", "ws"] }
axum-extra = { version = "0.10", features = ["query"] }
axum-macros = "0.5.0-alpha.1"
tower = { version = "0.5", features = ["util"] }
tower-http = { version = "0.7.0", features = ["cors", "fs", "catch-panic", "trace"] }
tower-cookies = "0.11"
cookie = "0.18"
http = "1.3.1"
http-body-util = "0.1"
hyper = { version = "1.5.2", features = ["full"] }

# ============================================================================
# HTTP Client
# ============================================================================
reqwest = { version = "0.12.15", features = ["json", "stream", "multipart", "rustls-tls"] }

# ============================================================================
# API Documentation (OpenAPI/Swagger)
# ============================================================================
utoipa = { version = "5.4.0", features = ["axum_extras", "debug"] }
utoipa-swagger-ui = { version = "9.0.2", features = ["axum", "vendored"], default-features = false }
schemars = { version = "1.2", features = ["preserve_order"] }
jsonschema = { version = "0.56.0", default-features = false }

# ============================================================================
# Cryptography & Security
# ============================================================================
argon2 = "0.6"
aes-gcm = "0.11.0"
hkdf = "0.13"
totp-rs = "6.0.0"
x25519-dalek = { version = "3.0", features = ["static_secrets"] }
sha2 = "0.11.0"
base64 = "0.23"
hex = "0.4.3"
# openidconnect 4.x — bumped from 3.x to clear three transitive
# rustls-webpki CVEs (RUSTSEC-2026-0098/0099/0104) that came in via
# oauth2 4 → reqwest 0.11 → rustls 0.21 → rustls-webpki 0.101. 4.x
# pulls oauth2 5 → reqwest 0.12 → rustls 0.23 → webpki 0.103.x
# (patched). API breaking changes: `discover_async` / `request_async`
# now take a `&reqwest::Client` directly instead of an
# `async_http_client` adapter — this lets us own the HTTP client and
# apply our own timeout, redirect policy, and SSRF defenses (see
# `oidc_http_client` in `crates/temps-auth/src/oidc_service.rs`).
#
# `rustls-tls` — required so the reqwest::Client we build can speak
# HTTPS. Without it, hyper-util's connector rejects every
# https:// URL with "invalid URL, scheme is not http".
#
# `accept-rfc3339-timestamps` — required for Auth0 compatibility:
# Auth0 returns `updated_at` in id_tokens as an ISO 8601 string
# (e.g. `"2026-05-24T11:07:42.800Z"`) when the upstream connection
# is Google/social, instead of the Unix-seconds number the OIDC
# spec requires. Without this feature openidconnect rejects the
# entire token response with `data did not match any variant of
# untagged enum Timestamp`.
openidconnect = { version = "4.0.1", default-features = false, features = ["reqwest", "rustls-tls", "accept-rfc3339-timestamps"] }

# ============================================================================
# TLS & Certificate Management
# ============================================================================
instant-acme = "0.8.5"
rcgen = { version = "0.14.8", features = ["x509-parser"] }
# RUSTSEC-2026-0285: reject TLS 1.3 handshake messages across key changes.
rustls = { version = "0.23.45", default-features = false, features = [
    "ring",
    "std",
    "tls12",
    "logging",
    "aws_lc_rs"
] }
rustls-pemfile = "2.0"
rustls-platform-verifier = "0.5"
# Async TLS for the agent's mTLS listener (ADR-020 WS-2.1). `ring` matches the
# process-default CryptoProvider installed by the CLI.
tokio-rustls = { version = "0.26", default-features = false, features = [
    "ring",
    "tls12",
    "logging",
] }
webpki-roots = "1.0"

# ============================================================================
# ClickHouse
# ============================================================================
# Canonical version/feature pin for the whole workspace. Four crates
# (temps-otel, temps-analytics-backend, temps-proxy, temps-metrics) currently
# vendor their own ClickHouse config/client/migration-runner code against this
# same version; temps-clickhouse is the shared crate those are meant to
# migrate onto. Keep this pin in lockstep with `crates/temps-clickhouse`.
clickhouse = { version = "0.15", features = ["uuid", "chrono"] }

# ============================================================================
# Reverse Proxy (Pingora)
# ============================================================================
pingora = { version = "0.9.0" }
pingora-core = { version = "0.9.0", features = ["openssl", "connection_filter"] }
pingora-proxy = { version = "0.9.0" }
prometheus = "0.14"

# ============================================================================
# External Service Integrations
# ============================================================================
# Cloud Storage (S3/MinIO)
# default-features = false drops the deprecated `rustls` feature, which transitively
# enables aws-smithy-runtime/tls-rustls → hyper-rustls 0.24 → rustls 0.21 →
# rustls-webpki 0.101.7 (RUSTSEC-2026-0098/-0099/-0104). The modern HTTPS path
# (default-https-client → rustls-aws-lc → rustls 0.23 + aws-lc-rs) is preserved.
aws-sdk-s3 = { version = "1.112.0", default-features = false, features = ["sigv4a", "http-1x", "default-https-client", "rt-tokio"] }
aws-config = { version = "1.6", default-features = false, features = ["default-https-client", "rt-tokio", "credentials-process", "sso"] }
# Caching (Redis)
redis = { version = "1.3.0", features = ["tokio-comp", "connection-manager"] }

# Email (SMTP)
lettre = { version = "0.11.11", features = ["tokio1", "tokio1-rustls-tls", "smtp-transport", "tokio1-native-tls"] }

# Git Providers (GitHub, GitLab)
octocrab = "0.54.1"

# ============================================================================
# Container Management (Docker)
# ============================================================================
bollard = { version = "0.20.1", features = ["buildkit", "buildkit_providerless", "chrono", "json_data_content"] }

# ============================================================================
# Scheduling & Background Jobs
# ============================================================================
cron = "0.17.0"

# ============================================================================
# String & URL Utilities
# ============================================================================
url = "2.5.4"
urlencoding = "2.1"
percent-encoding = "2.3"
slug = "0.1.6"
regex = "1.11"

# ============================================================================
# Compression & Archive
# ============================================================================
flate2 = "1.0"
tar = "0.4.45"
walkdir = "2"
include_dir = "0.7"
zip = "8.6"
zstd = "0.14"

# ============================================================================
# CLI Framework
# ============================================================================
clap = { version = "4.4", features = ["derive", "env"] }

# ============================================================================
# System & Utilities
# ============================================================================
bytes = "1.11.1"
libc = "0.2"
# jemalloc global allocator (temps-cli bin only): low fragmentation + time-based
# decay returns freed pages to the OS. "profiling"/"stats" are inert unless
# _RJEM_MALLOC_CONF is set at process start (see temps-cli/src/main.rs).
tikv-jemallocator = { version = "0.7", features = ["profiling", "stats"] }
tokio-tungstenite = { version = "0.30", default-features = false, features = ["connect", "rustls-tls-webpki-roots"] }
rand = "0.10"
sysinfo = "0.39.6"
tempfile = "3.12.0"
once_cell = "1.20"
notify = "8.2"

# ============================================================================
# User Agent & Analytics
# ============================================================================
woothee = "0.13.0"

# ============================================================================
# Testing
# ============================================================================
# Using git version for security fixes:
# - Uses astral-tokio-tar instead of vulnerable tokio-tar (RUSTSEC-2025-0111)
# - Compatible with bollard 0.19.x
testcontainers =  "0.27.2"


[profile.release]
codegen-units = 1
opt-level = 3

# Fast non-debug build for local "just run it" workflows. Inherits release
# semantics but parallelizes codegen, drops debug info, and strips symbols
# so link time is a fraction of `--release`. Use: `cargo run --profile fast`.
[profile.fast]
inherits = "release"
codegen-units = 16
debug = false
strip = "symbols"
lto = false
incremental = true

[profile.dev]
opt-level = 0
debug = "line-tables-only"
incremental = true
split-debuginfo = "unpacked"

[profile.dev.package."*"]
opt-level = 1
debug = false

# ============================================================================
# Dependency Overrides - Security Fixes
# ============================================================================
# Known vulnerabilities from transitive dependencies (blocked on upstream):
#
# 1. RUSTSEC-2024-0437 / GHSA-2gh3-rmm4-6rq5 / CVE-2025-53605: protobuf 2.28.0 (MEDIUM)
#    - RESOLVED 2026-09-14: bumped the whole pingora crate family to 0.9.0
#      (pingora, pingora-core, pingora-proxy, pingora-http, pingora-load-balancing).
#      0.9.0 carries the prometheus split (pingora main @402acae) into a separate
#      `pingora-prometheus` crate declaring `prometheus = "0.14"` (protobuf 3.x),
#      so pingora-core no longer pulls prometheus 0.13 / protobuf 2.28.0 at all.
#      See PR that bumped the family together (supersedes 5 single-crate Dependabot
#      PRs that each failed CI because Dependabot proposed bumping one pingora
#      crate at a time, leaving the rest pinned to 0.8.1 and breaking compilation).
#
# 2b. GHSA-w9wp-h8wv-79jx: opentelemetry_sdk 0.30.0 (MEDIUM, dependabot-only)
#    - Path: relay-event-schema (git pin getsentry/relay) → opentelemetry-proto 0.30
#      → opentelemetry_sdk 0.30.0
#    - Fix: needs opentelemetry_sdk >= 0.32.1, which requires opentelemetry-proto
#      0.32. relay-event-schema enables `opentelemetry-proto = ["gen-tonic","trace"]`
#      and the `trace` feature is what drags in opentelemetry_sdk; there is no
#      [patch] override possible (0.30 -> 0.32 is semver-incompatible).
#    - Upstream status (checked 2026-08-04): BUMPING THE RELAY REV DOES NOT FIX THIS.
#      relay master @3215baa is on opentelemetry-proto 0.31, and 0.31's `trace`
#      feature still resolves opentelemetry_sdk 0.31 -- below the 0.32.1 fix
#      (verified by probe build). So the block is not "we haven't moved the pin":
#      relay must first adopt opentelemetry-proto 0.32. Moving the pin 897 commits
#      forward would buy relay-protocol API churn and still leave the alert open.
#    - Reachability: the unbounded-alloc bug is in the W3C BaggagePropagator. relay
#      pulls opentelemetry-proto only for OTLP span TYPE conversion; the propagation
#      module is compiled in (trace feature) but BaggagePropagator is never
#      instantiated and .extract() is never called on attacker headers anywhere in
#      the workspace -> not reachable. (re-verified 2026-08-04: no case-insensitive
#      match for `baggage` anywhere under crates/.)
#
# 3. GHSA-xxx: libcrux-intrinsics 0.0.3 (HIGH severity, dev-dependency only)
#    - Path: testcontainers → russh → libcrux-ml-kem → libcrux-intrinsics 0.0.3
#    - Fix: Requires russh to upgrade libcrux dependencies
#    - Status: Dev-dependency only, zero production impact
#
# 5. RUSTSEC-2026-0235: rkyv 0.7.46 (out-of-bounds read, lockfile-only)
#    - Path: sea-orm 1.1.20 → rust_decimal 1.42.0 → rkyv 0.7.46 (OPTIONAL)
#    - Fix: patched in rkyv 0.8.17; upstream no longer supports the 0.7 series,
#      and rust_decimal 1.42.1 (latest) still declares `rkyv ^0.7.46`. A
#      [patch.crates-io] to 0.8 is semver-incompatible and rejected. Removing
#      the entry from Cargo.lock by hand does not stick -- cargo re-adds it on
#      the next resolve. Total upstream block until rust_decimal bumps it.
#    - Reachability: NOT COMPILED AT ALL. rkyv is an optional feature of
#      rust_decimal that nothing in this workspace enables. It appears in
#      Cargo.lock because cargo resolves the lock over the maximal feature set,
#      but `cargo tree --target all -e normal` has zero occurrences of rkyv
#      (verified 2026-08-04, contrast rust_decimal itself with 9). The
#      vulnerability requires validating an untrusted archive containing
#      Rc/Arc; temps neither compiles nor calls rkyv.
#    - Tooling note: this is a false positive of the gate, not of the advisory.
#      cargo-audit reads Cargo.lock rather than the built artifact. Moving the
#      job to cargo-auditable + `cargo audit bin` would remove the need for
#      this exception entirely, and is the preferred long-term fix.
#
# 4. astral-tokio-tar: RESOLVED by upgrading testcontainers 0.27.1 → 0.27.2
#
# 5. RUSTSEC-2026-0253: lru 0.16.4 (unsound use-after-free in LruCache::pop())
#    - Path: aws-sdk-s3 1.138.1 → lru 0.16.4 (aws-sdk-s3 declares `lru = "^0.16.3"`)
#    - Fix: patched in lru 0.18.2.
#    - Upstream status (checked 2026-08-24): BLOCKED. aws-sdk-s3 1.143.0
#      (crates.io latest as of that date) still declares `lru = "^0.16.3"` --
#      confirmed via `cargo update -p aws-sdk-s3 --precise 1.143.0 --dry-run
#      --verbose`, which does not touch the lru entry. No newer aws-sdk-s3
#      release has moved past lru 0.16 yet. A [patch.crates-io] override to
#      0.18.2 would violate aws-sdk-s3's own semver requirement and risks
#      breaking whatever internal caching it does with the crate.
#    - Reachability: the bug requires a panicking `Drop` impl on a cache key
#      type, unwound through `catch_unwind`, during `LruCache::pop()`. Not
#      yet audited which key type aws-sdk-s3/aws-smithy-runtime uses for this
#      cache or whether panicking-Drop keys are reachable from request
#      handling -- re-verify this before renewing the exception long-term.
#
# Previously resolved:
# - atty 0.2.14: RESOLVED in Pingora 0.7.0 (upgraded to clap 4.5)
# - idna 0.2.3: RESOLVED by upgrading check-if-email-exists 0.9 → 0.11
# - lru 0.12.5: RESOLVED by upgrading aws-sdk-s3 (new version dropped lru 0.12)
# - get_if_addrs 0.5.3 (RUSTSEC-2025-0121, gcc build-dep unmaintained):
#     RESOLVED by replacing get_if_addrs with the maintained if-addrs 0.15.0
#     fork in temps-infra (single call site, platform_info.rs). Same API
#     (`if_addrs::get_if_addrs()`, `Interface.name/.ip()/.is_loopback()`),
#     depends only on libc/windows-sys -- no C build step, so gcc/cc drop
#     out of the tree entirely.
# - aws-lc-sys 0.38.0 (RUSTSEC-2026-0044, RUSTSEC-2026-0048): RESOLVED by cargo update aws-lc-rs → 1.16.2
# - rustls-webpki 0.103.7 (RUSTSEC-2026-0049): RESOLVED by cargo update rustls-webpki → 0.103.10
# - tar 0.4.44 (RUSTSEC-2026-0067, RUSTSEC-2026-0068): RESOLVED by bumping tar to 0.4.45
# - rkyv 0.7.45 (RUSTSEC-2026-0001): RESOLVED by cargo update rkyv → 0.7.46
# - rustls-webpki 0.103.10 (RUSTSEC-2026-0098, -0099, -0104): RESOLVED by cargo update rustls-webpki@0.103 → 0.103.13
# - rand 0.9.2 (RUSTSEC-2026-0097): RESOLVED by cargo update rand@0.9 → 0.9.4
# - rand 0.8.5 (RUSTSEC-2026-0097): RESOLVED by cargo update rand@0.8 → 0.8.6
# - postgres-protocol 0.6.11 (RUSTSEC-2026-0179 unbounded-SCRAM-iteration CPU
#     exhaustion DoS; RUSTSEC-2026-0180 hstore decode panic DoS): RESOLVED by
#     cargo update postgres-protocol → 0.6.12. Network-reachable: temps connects to
#     user-managed external Postgres servers, so a malicious/compromised server
#     could trigger these against the temps process.
# - tokio-postgres 0.7.17 (RUSTSEC-2026-0178 DataRow-with-too-few-fields panic
#     DoS): RESOLVED by cargo update tokio-postgres → 0.7.18 (pulls postgres-types
#     → 0.2.14). Same user-managed-server reachability as above.
# - quinn-proto 0.11.14 (RUSTSEC-2026-0185 remote memory exhaustion via unbounded
#     out-of-order stream reassembly): RESOLVED by cargo update quinn-proto →
#     0.11.15. (Orphaned lock entry with no active dependents — bumped for hygiene
#     so `cargo audit` stays clean.)
# - remove_dir_all 0.5.3 (RUSTSEC-2023-0018 / GHSA-mc8h-8q98-g5hr) and
#     serde_with 2.3.3 (GHSA-8wf9-4rjw-8j9r): RESOLVED by replacing the nixpacks
#     build engine with autopack. Both reached the tree only through
#     nixpacks 1.41.0 (→ tempdir 0.3.7, and → serde_with ^2.1.0 respectively);
#     neither had an upstream fix available, so removing the dependency was the
#     only way to clear them. autopack pulls neither.
# - serde_with 3.20.0 (GHSA-8wf9-4rjw-8j9r KeyValueMap serialize panic on empty
#     sequence/map entries): RESOLVED by cargo update serde_with@3.20.0 --precise
#     3.21.0 (pulls serde_with_macros → 3.21.0). Reached via cloudflare 0.14 →
#     temps-dns. The 2.x copy came from nixpacks and left with it.
# - rustls-webpki 0.101.7 (RUSTSEC-2026-0098, -0099, -0104) on rustls 0.21:
#     RESOLVED by disabling default features on aws-sdk-s3, aws-sdk-sesv2, and
#     aws-config so the deprecated `rustls` feature (which activates
#     aws-smithy-runtime/tls-rustls → hyper-rustls 0.24 → rustls 0.21 →
#     rustls-webpki 0.101.7) is no longer enabled. The modern path
#     (default-https-client → rustls-aws-lc → rustls 0.23 + aws-lc-rs) is kept.
#
# 5. RUSTSEC-2023-0071: rsa 0.9.10 (MEDIUM severity)
#    - Title: Marvin Attack: potential key recovery through timing sidechannels
#    - Dependency paths:
#        jsonwebtoken 10.4.0 <- octocrab      (GitHub App JWT signing;
#          crates/temps-git/src/services/github.rs,
#          crates/temps-git/src/services/git_provider_manager.rs)
#        jsonwebtoken 10.4.0 <- openidconnect (OIDC id_token verification)
#        rsa direct usage in crates/temps-dns/src/providers/gcp.rs
#          (GCP Cloud DNS service-account JWT signing, sign_rs256())
#    - Status: no fixed version exists upstream (the rsa crate has no constant-time
#      release yet). Tracked.
#    - Assessment: the Marvin Attack is an adaptive chosen-ciphertext attack against
#      RSA-PKCS#1v1.5 *decryption*. Temps has zero RSA decryption usage -- all three
#      paths above are RSA *signing* only (OIDC verification is a separate,
#      unaffected public-key op). Signing uses a non-constant-time modexp, which is
#      a real but much narrower timing side-channel than Marvin's decryption
#      oracle: exploiting it needs millions of samples at nanosecond precision, the
#      signing op itself (~10-50us) is buried inside git/DNS operations dominated
#      by 100-500ms of network I/O, and the signed payload (app_id/iat/exp or
#      service-account claims) isn't attacker-controlled. Blast radius if ever
#      exploited is per-customer only: each org supplies its own GitHub App or GCP
#      service-account private key, not a shared temps platform secret.
#    - Follow-up: monitor upstream `rsa` for a constant-time release; evaluate
#      ring/aws-lc-rs-backed signing as a replacement for both paths.
#
# All remaining items are transitive dependencies from third-party crates that we
# cannot directly patch without forking. Monitoring upstream for updates.
